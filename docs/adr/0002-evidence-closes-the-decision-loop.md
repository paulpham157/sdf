# Evidence closes the decision loop

Every evaluator run creates append-only Evidence that can validate or contradict an Assumption or Decision. The evaluator, not the agent, determines the result; this preserves the SDF thesis that business intent is testable through engineering and runtime evidence.
